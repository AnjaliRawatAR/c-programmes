//to find the square root of any number

#include <stdio.h>
#include <math.h>
void main()
{
    int x;
    printf("Enter the number:\n");
    scanf ("%d", &x);
    printf ("the number is :%d",x);
    printf ("\nthus the Square root is: %f",sqrt(x));
}
